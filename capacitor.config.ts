import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rsinnotech.inonic-angular',
  appName: 'ionic-angular-course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
